rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas temporales para desarrollo - PERMITIR TODO A USUARIOS AUTENTICADOS
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
    
    // Reglas específicas para producción (comentadas por ahora)
    /*
    function isSignedIn() {
      return request.auth != null;
    }

    function isAdmin() {
      return isSignedIn() && (
        (exists(/databases/$(database)/documents/coaches/$(request.auth.uid)) && get(/databases/$(database)/documents/coaches/$(request.auth.uid)).data.role == 'admin') ||
        (exists(/databases/$(database)/documents/players/$(request.auth.uid)) && get(/databases/$(database)/documents/players/$(request.auth.uid)).data.role == 'admin')
      );
    }

    // Tickets de soporte
    match /tickets/{ticketId} {
      allow read: if isSignedIn() && (
        resource.data.userId == request.auth.uid || isAdmin()
      );
      // Sugerencia: bloquear escritura desde cliente y escribir solo vía Admin SDK (API)
      allow create: if false; // Crear tickets solo desde servidor
      allow update, delete: if false; // Actualizaciones solo desde servidor
    }

    // Subcolección de mensajes de tickets (lectura por dueño o admin)
    match /tickets/{ticketId}/messages/{messageId} {
      allow read: if isSignedIn() && (
        get(/databases/$(database)/documents/tickets/$(ticketId)).data.userId == request.auth.uid || isAdmin()
      );
      allow create, update, delete: if false; // Escrituras por servidor
    }

    // Reglas para usuarios autenticados
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Reglas para coaches
    match /coaches/{coachId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == coachId;
    }
    
    // Reglas para jugadores
    match /players/{playerId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && (
        request.auth.uid == playerId || 
        resource.data.coachId == request.auth.uid
      );
    }
    
    // Reglas para análisis de tiros
    match /shotAnalysis/{analysisId} {
      allow read: if request.auth != null && (
        resource.data.playerId == request.auth.uid ||
        resource.data.coachId == request.auth.uid
      );
      allow create: if request.auth != null;
      allow update, delete: if request.auth != null && (
        resource.data.playerId == request.auth.uid ||
        resource.data.coachId == request.auth.uid
      );
    }
    
    // Reglas para ejercicios personalizados
    match /drills/{drillId} {
      allow read: if request.auth != null && (
        resource.data.playerId == request.auth.uid ||
        resource.data.coachId == request.auth.uid
      );
      allow create: if request.auth != null;
      allow update, delete: if request.auth != null && (
        resource.data.playerId == request.auth.uid ||
        resource.data.coachId == request.auth.uid
      );
    }
    
    // Reglas para progreso del jugador
    match /playerProgress/{progressId} {
      allow read: if request.auth != null && (
        resource.data.playerId == request.auth.uid ||
        resource.data.coachId == request.auth.uid
      );
      allow write: if request.auth != null && (
        resource.data.playerId == request.auth.uid ||
        resource.data.coachId == request.auth.uid
      );
    }
    */
  }
}
